function bars(el,items,valueKey,danger=false){const max=Math.max(...items.map(x=>Math.abs(x[valueKey])),1);el.innerHTML=items.map(x=>`<div class="bar-row"><span class="bar-label">${x.Label}</span><div class="bar-track"><div class="bar-fill ${danger?'danger':''}" style="width:${Math.max(2,Math.abs(x[valueKey])/max*100)}%"></div></div><span class="bar-value">${Number(x[valueKey]).toLocaleString('vi-VN',{maximumFractionDigits:2})}</span></div>`).join('')}
function donut(el,total,risks){const c=risks.Critical/total*100,w=risks.Warning/total*100;el.innerHTML=`<div class="donut" style="background:conic-gradient(var(--red) 0 ${c}%,var(--orange) ${c}% ${c+w}%,var(--green) ${c+w}% 100%)"><div class="donut-center"><strong>${total}</strong><small>TỔNG SỐ NÚT</small></div></div>`}
